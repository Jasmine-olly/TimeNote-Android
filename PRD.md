# TimeNote 产品需求文档 (PRD)

> 版本：v1.0（已实现交付）
> 日期：2026-08-03
> 状态：已实现（2026-08-05 正式版 v1.0.0 交付，功能与本文档对齐）

---

## 1. 产品概述

### 1.1 一句话定位
**TimeNote 是一个「温柔监督 + 定时自省 + 日记沉淀」的自我认知工具**，帮你看见时间花在了哪里、记住每一天的感受。

### 1.2 目标用户
本人（个人使用为主），关注时间管理与自我觉察的手机用户。

### 1.3 核心价值主张
- **温柔地提醒**：不用强制封锁，而是让「时间流逝」可见，把选择权还给你。
- **定时地问**：在设定的时间点提出预设问题，采集当下的感受，形成连续的自我记录。
- **完整地沉淀**：每天的使用数据 + 感受碎片自动汇编成一篇日记，可手动编辑，像翻实体日记本一样回看。

---

## 2. 平台与运行环境

| 项 | 决策 |
|---|---|
| 目标平台 | Android 手机 |
| 最低系统版本 | Android 8.0 (API 26) 起（覆盖绝大多数机型） |
| 数据存储 | 纯本地存储，零联网、零上传（隐私红线） |

---

## 3. 核心功能需求

### F1 娱乐计时器

**目标**：监控用户使用「娱乐类 App」的时长，提供可见性并适时提醒。

#### F1.1 娱乐应用清单
- 用户**手动勾选**手机中的哪些 App 属于「娱乐应用」。
- 勾选后长期生效，可随时增删。
- 清单持久化保存。

#### F1.2 悬浮计时器
- 当检测到当前前台是已勾选的娱乐 App 时，屏幕常驻显示一个**悬浮计时器**，实时显示本次连续使用时长（如 `已使用 12:34`）。
- 悬浮窗平时不打扰，仅静默显示。
- 切换出娱乐 App 或锁屏后，计时停止/重置（进入再重新开始）。

#### F1.3 阈值弹窗
- 用户可设置提醒阈值（默认建议：30 / 60 / 90 分钟）。
- 使用时长达到阈值时弹出**提醒弹窗**。
- 弹窗内容：`已连续使用娱乐应用 30 分钟`。
- 弹窗带 3 个操作按钮：
  1. **【再玩5分钟】**：收起弹窗，5 分钟后再次提醒（本次阈值再延 5 分钟）。
  2. **【退出应用】**：进入「告别引导页」（见 F1.4）。
  3. **【关闭提醒】**：本次提醒不再打扰，但悬浮计时器继续。

#### F1.4 退出引导页（降级方案）
> 技术现实：Android 第三方 App **无法强制关闭其他 App**，此为目标系统级权限。
- 点【退出应用】→ 展示「该走了」引导页，提示用户手动返回/退出当前娱乐 App。
- 引导页带 **10 秒倒计时**，期间静默监督（若用户已切出则祝贺，若仍在使用则温和提示）。
- 倒计时结束自动收起。

#### F1.5 权限要求
- **悬浮窗权限**（SYSTEM_ALERT_WINDOW）：悬浮计时器 + 阈值弹窗使用。
- **使用情况访问权限**（UsageStats / PACKAGE_USAGE_STATS）：检测前台应用与时长。
- **通知/无障碍权限**（备选）：用于前台应用检测的兜底方案（部分场景 UsageStats 检测有延迟）。

---

### F2 定时提问

**目标**：在用户设定的时间点，弹出预设问题，采集当下的回答。

#### F2.1 问题管理
- 用户**自定义「时间 → 问题」配对**，例如：
  - `08:30 → 今天早餐吃了什么？`
  - `22:00 → 今天有什么后悔的事吗？`
  - `14:00 → 此刻你的专注度如何？`
- 每个时间点对应**一个**问题（不同时间不同问题）。
- 支持新增 / 编辑 / 删除 / 暂停某条问题计划。
- 可设置每周重复（每日 / 工作日 / 自定义星期）。

#### F2.2 到点弹窗提问
- 到达设定时间，弹出提问卡片（即使当前在其他 App 上，以悬浮层/通知形式呈现）。
- 用户以**自由文本**作答。
- 作答自动附带**场景信息**：作答时间、当前正在使用的 App（可选勾选）。

#### F2.3 错过补答
- 若到点时用户没空（锁屏、开会等）：
  - 该问题进入**「待回答」列表**，挂起。
  - 用户打开 TimeNote 时可见待回答提醒，可随时补答。
- 补答记录标注原始应答时间与补答时间。

---

### F3 日记本

**目标**：将一天的使用数据 + 感受碎片，汇编成一篇可翻阅、可编辑的日记。

#### F3.1 自动汇编
- 每天结束（或用户主动触发）自动生成当日日记，内容包括：
  1. **日期 / 天气（可手动填）**
  2. **娱乐使用统计**：各娱乐 App 使用时长、总时长、高峰时段。
  3. **定时提问的回答**：按时序排列（含问题、回答、场景信息）。
  4. **当日小结**：自动生成的一段话总结（基于当天记录）。

#### F3.2 手动编辑
- 用户可在自动汇编的基础上**增 / 删 / 改**任意段落（像编辑一篇文档）。
- 编辑内容持久化。

#### F3.3 翻阅体验（封面索引 + 日历 + 左右翻页）
- **日历式入口**：月视图日历，有日记的日期显示小圆点，点日期打开当日日记。
- **封面索引**：每篇日记可设置封面（如天气图标 / 心情封面），翻阅时先看到索引封面。
- **左右翻页**：支持像实体日记本一样左右滑动翻页（一页一天）。

---

## 4. 数据与隐私设计（红线）

| 项 | 决策 |
|---|---|
| 存储位置 | 全部本地存储（Room / SQLite） |
| 联网 | **禁止任何网络请求**，无账号、无云端、无统计上报 |
| 数据文件 | 使用情况、问题计划、作答记录、日记，全部本地 |
| 导出 | 手动一键导出 Markdown / JSON 到手机本地，由用户自行管理 |
| 备份 | 不接入云端备份（符合隐私第一原则） |

> 注：导出为用户主动操作，需在导出界面明确提示文件将写入手机本地路径。

---

## 5. 技术方案要点

### 5.1 技术选型（建议）
| 层 | 方案 |
|---|---|
| 语言 / UI | Kotlin + Jetpack Compose |
| 数据库 | Room（本地 SQLite） |
| 后台定时 | WorkManager / AlarmManager（定时提问、每日汇编） |
| 前台检测 | UsageStatsManager + AccessibilityService 兜底 |
| 悬浮窗 | SYSTEM_ALERT_WINDOW（需 Android 13+ 动态申请） |

> 选型理由：本项目深度依赖 Android 系统级能力（悬浮窗、使用统计、前台检测），**原生 Kotlin 是最可靠的选择**，跨平台框架（Flutter/RN）在这些 API 上支持弱、维护难。

### 5.2 关键挑战与对策
| 挑战 | 对策 |
|---|---|
| 国产 ROM（小米/华为/OPPO/vivo）对悬浮窗/自启动有额外限制 | 引导页提供「手动开启权限」图文指引；测试覆盖主流 ROM |
| 全屏游戏/视频时悬浮窗可能被系统遮挡 | 降级为系统通知栏提醒；文档明示限制 |
| UsageStats 检测有延迟（数秒~数十秒） | 用 AccessibilityService 实时兜底前台检测 |
| 无法强制关闭其他 App | F1.4 引导页降级方案（已与用户确认） |

---

## 6. 版本规划

### MVP（V1.0）— 核心闭环
- F1 娱乐计时器完整版（清单 + 悬浮计时 + 阈值弹窗 + 引导页）
- F2 定时提问完整版（自定义问题 + 到点弹窗 + 待回答补答）
- F3 日记自动汇编 + 日历翻阅（含基础手动编辑）
- 本地存储、无网络

### V1.1 — 体验完善
- 封面索引 + 左右翻页翻阅
- 每周重复规则细化
- 当日小结增强

### V1.2 — 扩展
- 手动导出 Markdown / JSON
- 数据统计可视化（周报 / 月报）

---

## 7. 未决事项（均已实现并验证，2026-08-05）
- ~~阈值弹窗的默认阈值与可配置项~~ → 已实现（可自定义多级阈值，默认 30 / 60 / 90）
- ~~「再玩5分钟」可重复次数上限~~ → 已实现（单次会话最多 3 次）
- ~~日记封面默认取值~~ → 已实现（天气自动 + 回答情绪关键词建议，可手动修改）
- ~~悬浮计时器的样式（大小 / 位置 / 拖动）~~ → 已实现（可拖动并记忆位置）

---

